# Where Our Paths Meet

Scroll-driven, single-user narrative experience across three emotional timelines with a custom SVG stickman system.

## Run locally (Windows / PowerShell)

1. Install deps:
   - `npm.cmd install`
2. Start dev server:
   - `npm.cmd run dev`

Open `http://localhost:3000`.
